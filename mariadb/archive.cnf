[mysqld]
port = 3307

# Character Set
character_set_server = utf8mb4
collation_server = utf8mb4_unicode_ci

# Large archived-request payloads (messages column can reach ~10 MB)
max_allowed_packet = 64M
